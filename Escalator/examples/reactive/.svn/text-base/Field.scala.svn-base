
import edu.depauw.reactive._
import edu.depauw.reactive.Predef._

runScript {
	val field = Field()
	val label = Label(if (field.value == "hello world") "Thanks!" else "Please type 'hello world'")
	Frame(label ^ field)
}

